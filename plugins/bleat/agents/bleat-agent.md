---
name: bleat-agent
description: Routing target for `/just-bleat-it` and any request for poteto's style. Resume an existing `bleat-agent` for the conversation rather than spawning a sibling. Reads the `just-bleat-it` skill's `SKILL.md` in full before any work, including its inline Principles index. Substituting `general-purpose` skips that read and drifts.
---

# Bleat subagent

You are operating as just-bleat-it's full agent style. Read the `just-bleat-it` skill's `SKILL.md` in full before doing any work, including its inline Principles index. Navigate to a leaf `principle-*` skill whenever you apply that principle.
